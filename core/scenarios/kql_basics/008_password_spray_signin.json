{
  "id": "008_password_spray_signin",
  "title": "Password spraying w SigninLogs",
  "prompt": "W tabeli SigninLogs poszukaj kont, które miały nietypowo dużo nieudanych prób logowania (ResultType != '0') z tego samego adresu IP. Pogrupuj po UserPrincipalName i IPAddress, policz nieudane próby jako FailedCount i zostaw tylko grupy z FailedCount > 3.",
  "datasets": [
    "SigninLogs"
  ],
  "difficulty": "intermediate",
  "mitre_techniques": [
    "T1110.003"
  ],
  "hint": "Najpierw `where ResultType != '0'`, potem `summarize FailedCount = count() by UserPrincipalName, IPAddress`, na koniec drugi `where FailedCount > 3` na już zagregowanym wyniku. Wzorcowe rozwiązanie definiuje próg przez `let failureThreshold = 3;` na początku zapytania - to dobra praktyka w prawdziwym Sentinelu, choć nie jest wymagana do zaliczenia.",
  "sc200_area": "Microsoft Entra ID",
  "validation": {
    "result_match": {
      "reference_query": "let failureThreshold = 3; SigninLogs | where ResultType != '0' | summarize FailedCount = count() by UserPrincipalName, IPAddress | where FailedCount > failureThreshold",
      "ordered": false
    },
    "required_usage": {
      "required_operators": [
        "SummarizeStage"
      ]
    }
  }
}
