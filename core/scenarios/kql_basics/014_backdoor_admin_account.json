{
  "id": "014_backdoor_admin_account",
  "title": "Backdoorowe konto administratora",
  "prompt": "W DeviceProcessEvents znajdź uruchomienie net.exe tworzące nowe konto lokalne (linia poleceń zawiera 'user' oraz '/add'). Zwróć DeviceName, AccountName, ProcessCommandLine i InitiatingProcessFileName.",
  "datasets": [
    "DeviceProcessEvents"
  ],
  "difficulty": "beginner",
  "mitre_techniques": [
    "T1136.001"
  ],
  "hint": "`FileName == 'net.exe'`, potem `ProcessCommandLine has 'user' and ProcessCommandLine contains '/add'` - '/add' zawiera znak niealfanumeryczny, więc `has` (dopasowanie całego tokenu) go nie złapie, trzeba użyć `contains`.",
  "source_url": "https://maciekgorzny.github.io/security-writeups/hunt-002.html",
  "sc200_area": "Microsoft Defender for Endpoint (MDE)",
  "validation": {
    "result_match": {
      "reference_query": "DeviceProcessEvents | where FileName == 'net.exe' | where ProcessCommandLine has 'user' and ProcessCommandLine contains '/add' | project DeviceName, AccountName, ProcessCommandLine, InitiatingProcessFileName"
    }
  }
}
