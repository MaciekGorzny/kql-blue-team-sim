"""FastAPI application layer: HTML pages + JSON API on top of `core`
(engine/datasets/scenarios). Nothing under `core/` imports anything from
here - `core` stays reusable by other future Blue Team Simulator modules.
"""
